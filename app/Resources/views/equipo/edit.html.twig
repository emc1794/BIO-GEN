{% extends 'base.html.twig' %}

{% block page_content %}
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2><b>Edicion de Equipo </b><small>Formulario</small></h2>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">

                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <input id="form_submit" type="submit" value="Guardar cambios"  class="btn btn-primary" />
                <a href="{{ path('equipo_index') }}" class="btn btn-default">Volver al listado</a>
                {{ form_end(edit_form) }}
            </div>
        </div>
    </div>
{% endblock %}
