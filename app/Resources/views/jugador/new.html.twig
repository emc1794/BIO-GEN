{% extends 'base.html.twig' %}

{% block page_content %}
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2><b>Registro de jugador </b><small>Formulario</small></h2>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">

                {{ form_start(form) }}
                {{form_row(form.nombre)}}
                {{form_row(form.paterno)}}
                {{form_row(form.materno)}}
                {{form_row(form.ci)}}
                {{form_row(form.fechaNacimiento)}}
                {% for child  in form.sexo %}
                    <p>
                        {{form_row(child,{'attr':{'class':'flat'}})}}
                    </p>
                {% endfor %}
                {{form_row(form.telefono)}}
                {{form_row(form.email)}}
                {{form_row(form.direccion)}}
                <input id="form_submit" type="submit" value="Registrar"  class="btn btn-primary" />
                <a href="{{ path('jugador_index') }}" class="btn btn-default">Volver al listado</a>
                {{ form_end(form) }}


            </div>
        </div>
    </div>
{% endblock %}
