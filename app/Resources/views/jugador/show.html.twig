{% extends 'base.html.twig' %}

{% block page_content %}
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2><b>Datos del jugador </b><small>Ver</small></h2>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                <div class="row">
                    <div class="col-lg-12 col-md-12 col-xs-12">
                        <table class="table table-striped table-bordered">
                            <tbody>
                                <tr>
                                    <th>Nº</th>
                                    <td><b>{{jugador.id}}</b></td>
                                    <th>Jugador</th>
                                    <td>{{jugador.nombreCompleto}}</td>
                                </tr>
                                <tr>
                                    <th>Sexo</th>
                                    <td><b>{{jugador.sexoString}}</b></td>
                                    <th>Fecha de Nac.</th>
                                    <td>{{jugador.fechaNacimientoString}}</td>
                                </tr>
                                <tr>
                                    <th>C.I</th>
                                    <td><b>{{jugador.ci}}</b></td>
                                    <th>Telefono</th>
                                    <td>{{jugador.telefono}}</td>
                                </tr>
                                <tr>
                                    <th>
                                        Opciones
                                    </th>
                                    <td colspan="4">
                                        <a href="{{path('jugador_edit',{id:jugador.id})}}" class="btn btn-warning btn-xs"> Editar</i></a>
                                        {{ form_start(delete_form) }}
                                        <input type="submit" value="Eliminar" class="btn btn-danger btn-xs">
                                        {{ form_end(delete_form) }}
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
